const initialState = {

}

function authReducer(state = initialState, action: any) {
    switch (action.type) {
        case '':
            return {

            }
        default:
            return state;
    }
}

export default authReducer;