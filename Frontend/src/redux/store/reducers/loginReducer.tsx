const initialState = {

}

function loginReducer(state = initialState, action: any) {
    switch (action.type) {
        case '':
            return {

            }
        default:
            return state;
    }
}

export default loginReducer;