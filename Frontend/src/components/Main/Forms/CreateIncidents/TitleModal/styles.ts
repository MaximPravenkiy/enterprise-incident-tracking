import styled from 'styled-components';

export const SpanCustom = styled.span`
    margin: 0 10px;
    font-weight: bold;
    font-style: italic;
    font-size: 1.2em;
    line-height: 1.2em;
    color: red;
`;
